model:
  latent_dim: 384
  action_dim: 4
  num_heads: 4
  num_layers: 4
  mlp_ratio: 4
training:
  learning_rate: 0.0001
  batch_size: 32
  sequence_length: 16
  num_epochs: 30
  batches_per_epoch: 50
  grad_clip: 1.0
  weight_decay: 0.0
  lambda_latent: 1.0
  lambda_reward: 0.5
  lambda_done: 0.5
seed: 42
device: cuda
run_tag: baseline
data:
  processed_dir: data/processed
  buffer_capacity: 200000
